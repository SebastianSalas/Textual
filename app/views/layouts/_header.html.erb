<header>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg shadow-md py-2 bg-red relative flex items-center w-full justify-between">
    <div class="px-6 w-full flex flex-wrap items-center justify-between", style= 'justify-content: flex-start;'>
      <div class="navbar-collapse collapse grow items-center" id="navbarSupportedContentY">
        <ul class="navbar-nav mr-auto lg:flex lg:flex-row ">
          <a class="font-medium text-red-600 leading-tight text-3xl" href=<%= root_path %> >Textual</a>
        </ul>
      </div>
      <div>
      <div class="navbar-collapse collapse grow items-center" id="navbarSupportedContentY">
        <ul class="navbar-nav mr-auto lg:flex lg:flex-row">
          <% if signed_in? %>
            <li class= "nav-item">
              <%= link_to "Historial", destroy_user_session_path, class:"nav-link block pr-2 lg:px-2 py-2 text-gray-600 hover:text-gray-700 focus:text-gray-700 transition duration-150 ease-in-out" %>
            </li>
            
            <li class="nav-item">
              <%= link_to "Cerrar sesión", destroy_user_session_path, class:"nav-link block pr-2 lg:px-2 py-2 text-gray-600 hover:text-gray-700 focus:text-gray-700 transition duration-150 ease-in-out"%>
            </li>
          <%else%>
          <%end%>
        </ul>
      </div>
      </div>
      </div>
      </div>
    </div>
  </nav>
</header>